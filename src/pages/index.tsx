import type { NextPage } from "next";
import { Container, Typography, Divider, Stack, Grid } from "@mui/material";

const Home: NextPage = () => {
	return (
		<Container>
		</Container>
	);
};

export default Home;
